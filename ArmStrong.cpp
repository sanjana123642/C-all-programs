#include <iostream>
using namespace std;
    int main() {
    int num, temp, digit, sum = 0;
    double result;
    cout<< "Enter a number: ";
    cin >> num;
    originalNum = num;
    // Count number of digits
    while(originalNum != 0) {
        originalNum /= 10;
        n++;    
}
    